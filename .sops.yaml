# Sops creation rules. Same age recipient as the iedora-iac repo —
# single operator key on the Mac (~/.config/sops/age/keys.txt), backed up
# in personal Bitwarden vault.
creation_rules:
  # Production env vars for apps/web — dotenv format, committed encrypted.
  # Source of truth for app-runtime secrets; Coolify UI is a derived
  # consumer (paste values after editing with `sops apps/web/.env.prod`).
  - path_regex: apps/web/\.env\.prod$
    age: age1867kx5pchlg2aty9tqhwvlyyjn6p2rmxlrn9czszusqjdu2acp9qakhet5
